import 'package:flutter/cupertino.dart';

class ImageSelectorView extends StatelessWidget {
  const ImageSelectorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('IMAGE SELECTOR');
  }
}
